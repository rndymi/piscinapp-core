spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/piscinappdb
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: create-drop

springdoc:
  api-docs:
    enabled: true

  swagger-ui:
    enabled: true

piscinapp:
  security:
    issuer: http://localhost:8080
